import React from 'react';
import Palette from './palette';
import Canvas from './canvas';

const Root = () => {
  return (
    <React.Fragment>
      <Palette />
      <Canvas />
    </React.Fragment>
  )
}

export default Root;
